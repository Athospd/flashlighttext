# Workflow derived from https://github.com/r-lib/actions/tree/v2/examples
# Need help debugging build failures? Start at https://github.com/r-lib/actions#where-to-find-help
on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

name: R-CMD-check

jobs:
  R-CMD-check:
    runs-on: ${{ matrix.config.os }}

    name: ${{ matrix.config.os }} (${{ matrix.config.r }})

    strategy:
      fail-fast: false
      matrix:
        config:
          #- {os: macos-latest,   r: 'release'}
          - {os: ubuntu-latest,   r: 'release'}

    env:
      GITHUB_PAT: ${{ secrets.GITHUB_TOKEN }}
      R_KEEP_PKG_SOURCE: yes

    steps:
      - uses: actions/checkout@v3

      - uses: r-lib/actions/setup-pandoc@v2
      
      - uses: jwlawson/actions-setup-cmake@v1
        with:
          cmake-version: "3.25.1"

      - uses: r-lib/actions/setup-r@v2
        with:
          r-version: ${{ matrix.config.r }}
          http-user-agent: ${{ matrix.config.http-user-agent }}
          use-public-rspm: true

      - uses: r-lib/actions/setup-r-dependencies@v2
        with:
          extra-packages: any::rcmdcheck
          needs: check
          
      - name: install boost
        run: |
          sudo apt-get -y update
          sudo apt-get -y install libboost-dev libboost-program-options-dev libboost-system-dev libboost-thread-dev libboost-test-dev
          
      # - uses: MarkusJx/install-boost@v2.4.1
      #   id: install-boost
      #   with:
      #     boost_version: 1.73.0

      # - name: Set Boost dir
      #   env:
      #     Boost_INCLUDE_DIR: ${{ steps.install-boost.outputs.BOOST_ROOT }}
      #   run: echo test $Boost_INCLUDE_DIR 
          
      - uses: r-lib/actions/check-r-package@v2
        env:
          Boost_INCLUDE_DIR: ${{ steps.install-boost.outputs.BOOST_ROOT }}
        with:
          upload-snapshots: true
          
      - name: Upload check results
        if: failure()
        uses: actions/upload-artifact@master
        with:
          name: ${{ matrix.config.os }}-r${{ matrix.config.r }}-results
          path: |
            check/mlpack.Rcheck/00check.log
            check/mlpack.Rcheck/00install.out
            
      - name: log
        run: cat /home/runner/work/flashlighttext/flashlighttext/check/flashlighttext.Rcheck/00check.log
      
      - name: log2
        run: cat ${PKG_NAME}.Rcheck/00install.out
