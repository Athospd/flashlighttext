test_that("LMState", {
  # lmstate1 = LMState$new()
  # lmstate1_state30 = lmstate1$child(30)
  # lmstate1_state20 = lmstate1$child(20)
  # lmstate1_stateERROR = lmstate1$child("a")
  # lmstate1$children
  # lmstate1$compare(lmstate1$children$`30`)
  # lmstate1$compare(lmstate1$children$`20`)
  # 
  # lmstate2 = LMState$new()
  # lmstate2_state1 = lmstate2$child(1)
  # lmstate2$children
  # 
  # lmstate2$compare(lmstate2$children$`1`)
  # lmstate1$compare(lmstate1$children$`30`)
  # lmstate1$compare(lmstate1$children$`20`)
  # lmstate1$compare(lmstate2$children$`1`)
  # 
  expect_no_error(stop("to do"))
})
